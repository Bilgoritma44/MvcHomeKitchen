@model MvcHomeKitchen.Models.Concrete.Class4
@{
    ViewBag.Title = "OlanBiten";
    Layout = "~/Views/Shared/ProfileLayout.cshtml";
}
<div class="container">
    <div class="row">
        <div class="col-md-8 offset-2">
            <h4 class="text-center" style="color:red">Takip Ettiğin Yazarların Postları</h4>
            @foreach (var y in Model.Deger1)
            {
                foreach (var x in Model.Deger2)
                {

              if(y.TakipEdilen==x.WriterId)
                    { 
        <div class="card">

            @*<div class="form-group" style="margin:5px;">
            <img src="@x.Writer.PhotoUrl" style="width:50px;height:50px;" class="rounded-circle" />
            <h5 class="mt-0">@x.Writer.Name @x.Writer.Surname</h5>
        </div>*@
            <div class="media">
                <img src="@x.Writer.PhotoUrl" alt="" class="rounded-circle" style="width:50px;height:50px; margin:5px;" />
                <div class="media-body">
                    <h6 class="mt-2" style="margin-left:5px;"><small class="text-muted">@x.Writer.Name @x.Writer.Surname</small></h6>
                    <h6 class="mt-2" style="margin-left:5px;"><small class="text-muted">@x.Writer.Email</small></h6>
                </div>
            </div>
            <div class="form-group text-md-center" style="margin:5px;">
                <h5 class="card-title" style="color:blueviolet">Yeni Bir Tarif: @x.Title</h5>
            </div>
            <div class="form-group text-center" style="margin:5px;">
                <img class="" src="@x.Url" style="width:500px;height:300px;" alt="Card image cap">
            </div>
            <div class="card-body">

                <p class="card-text">@x.Context</p>
                <p class="card-text"><small class="text-muted">@x.Person Kişilik, @x.Preparation Hazırlık, @x.Cooking Pişirme</small></p>
            </div>
        </div>
        <br/>
                        break;
                    }
                }
            }
        </div>
    </div>
</div>